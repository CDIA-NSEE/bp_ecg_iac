FROM apache/airflow:3.1.6
# Pre-install the DockerOperator provider so the scheduler starts without
# the slow _PIP_ADDITIONAL_REQUIREMENTS pip install on every container start.
RUN pip install --no-cache-dir apache-airflow-providers-docker
